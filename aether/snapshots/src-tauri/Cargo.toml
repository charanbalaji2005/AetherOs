[package]
name = "aether-snapshots"
version = "1.0.0"
description = "AetherOS Disaster Recovery & Btrfs Snapshot Manager"
authors = ["AetherOS Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5", features = [] }

[dependencies]
tauri = { version = "1.5", features = ["api-all"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
custom-protocol = ["tauri/custom-protocol"]
